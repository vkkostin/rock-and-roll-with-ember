{{#each stars as |star|}}
  <span role="button" class="rating-star" onclick={{action "setRating" star.rating}} data-test-rr={{concat "star-rating-" star.rating}}>
    {{#if star.isFull}}
      {{fa-icon "star"}}
    {{else}}
      {{fa-icon "star-o"}}
    {{/if}}
  </span>
{{/each}}